server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /unihome
  forward-headers-strategy: framework
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/unihome}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:12345}
      timeout: 5s
  #      host: ${REDIS_HOST:redis-19053.c252.ap-southeast-1-1.ec2.cloud.redislabs.com}
  #      port: ${REDIS_PORT:19053}
  #      password: ${REDIS_PASSWORD:FLA8lunuAdoznwkrsda9AZn7qtUkFYc4}
  #      timeout: 5s
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:rosettyluvhenny@gmail.com}
    password: ${MAIL_PASSWORD:abil fxeb hagz wvwi}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    baseline-on-migrate: true
    locations: classpath:db/migration
    schemas: unihome
    table: flyway_schema_history
  jpa:
    show-sql: ${JPA_SHOW_SQL:false}
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    properties:
      hibernate:
        default_schema: unihome
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:723311125807-f8620lu6dg8j0ofjjlcukrhi22iq7g8c.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            scope: openid,profile,email

jwt:
  signerKey: ${JWT_SIGNER_KEY:your-super-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm}
  valid-duration: ${JWT_VALID_DURATION:36000}
  refreshable-duration: ${JWT_REFRESHABLE_DURATION:360000}

openapi:
  title: API document
  version: v1.0.0
  description: API service
  serverUrl: ${OPENAPI_SERVER_URL:http://localhost:8080/unihome}
  serverName: ${OPENAPI_SERVER_NAME:Localhost Dev Server}

springdoc:
  default-produces-media-type: application/json
  default-consumes-media-type: application/json
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: method
    tags-sorter: alpha
    doc-expansion: none
SENDGRID_API_KEY: ${SENDGRID_API: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}

FE_URL: ${PAGE_URL:http://localhost:8080/unihome}

app:
  warehouse:
    address: "UniHome Warehouse, Thu Duc City"
    latitude: 10.875056
    longitude: 106.800667
  shipping:
    free-threshold: 1000000
