name: CI

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      # Checkout source
      - name: Checkout repository
        uses: actions/checkout@v4

      #  Setup Java + cache Maven dependencies
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      #  Build JAR
      - name: Build JAR
        run: mvn -B clean package -DskipTests

      #  Run tests
      - name: Run tests
        env:
          SPRING_PROFILES_ACTIVE: test
          FLYWAY_ENABLED: false
        run: mvn -B test

